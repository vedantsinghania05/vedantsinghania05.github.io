---
import { Button } from "@/components/ui/button";
import DefaultLayout from "@/layouts/DefaultLayout.astro";
const { title, header, subheader, btntext, link } = Astro.props;

---

<DefaultLayout title={title}>
  <div
    class="flex flex-col h-[75vh] text-center justify-center items-center fade"
  >
    <div class="text-[4rem] text-center">
      {header}
    </div>
    <h1 class="">{subheader}</h1>
    <br />
    <a href={link}>
      <Button>
        <p class="text-xl">{btntext}</p>
      </Button>
    </a>
  </div>
</DefaultLayout>